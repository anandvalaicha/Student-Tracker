[build]
  command = "npm run build"
  publish = "dist"

# Send every request that isn't a static file back to index.html
# so React Router (or your manual page state) handles the route.
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
